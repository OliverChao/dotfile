local gitsigns = require("plugins.git.gitsigns")
local diffview = require("plugins.git.diffview")
local conflict = require("plugins.git.git-conflict")
return {
    gitsigns,
    diffview,
    conflict,
}
